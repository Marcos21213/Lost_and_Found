# Backend

Backend workspace placeholder.

The Python virtual environment is located at `.venv/` and is ignored by Git.

Activate it on Windows PowerShell:

```powershell
.\.venv\Scripts\Activate.ps1
```
